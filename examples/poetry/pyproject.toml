[tool.poetry]
name = "example"
version = "0.1.0"
description = ""
readme = "README.md"
authors = ["A <A@A.com>"]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
